# Write a script that takes files from data and turns them into rows for a pandas dataframe

# Goal: For every bill in data, create a row in a pandas dataframe with details such as "bill text", "date-created", "authors", etc.

# Tips: use string methods and regex for parsing. 